{
    "dependencies": {
        "gulp": "^4.0.2",
        "yarn": "^1.22.19"
    },
    "scripts": {
        "start": "npm install yarn -g --save && yarn set version berry && yarn install"
    },
    "packageManager": "yarn@3.3.0"
}
